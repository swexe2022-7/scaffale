 <P><%= Bookshelf.find(session[:shelf_id]).title %></P>
<% if @shelfdata.empty? %>
    <%= "本がありません" %><br>
<% else %>
    <% @shelfdata.each do |d| %>
        <% data=googlebookapi_search_one(d.googlebooksid) %>
        <%= data.title %><br>
        <%= link_to image_tag("#{data.thumbnail}"), "#{data.link}" %><br>
        <%= link_to 'delete', "/#{d.id}/delete" %><br>
    <% end %>
<% end %>

<%= link_to 'add book', "/add" %>